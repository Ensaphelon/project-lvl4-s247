import React from 'react';

const ChannelsRenameForm = (props) => {
  const { visible } = props;
  if (!visible) {
    return null;
  }
  return (
    <div>
      123
    </div>
  );
};

export default ChannelsRenameForm;
